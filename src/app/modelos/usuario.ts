// Definimos unha interface de implementación que nos dará a estructura común dos obxectos de tipo "usuario"
export interface Usuario {
    usuario: string;
    contrasinal: string;
    rol: string;
}

