// Definimos unha interface de implementación que nos dará a estructura común dos obxectos de tipo "pivot"
export interface PivotProducto {
    cantidad_consumida:number;
    cantidad_producida:number;
}